# Summary report preview

The current pull request contains a lightweight D3 proof of concept for `create_summary_report()`.

[Open the generated summary report](../summary-report-demo.html)

The report prepares the shared binary performance data once and reuses it for ROC, precision–recall, gains, lift, and decision-curve panels.
